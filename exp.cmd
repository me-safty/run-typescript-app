npm i express morgan dotenv && npm i -D @types/express @types/morgan nodemon @types/morgan && echo "import express from "express";import morgan from "morgan";import config from "./config";const port = config.PORT || 3000;const app = express();app.use(express.json());app.use(morgan("common"));app.get("/", (req: express.Request, res: express.Response) => {  res.json({    message: "hello, world",  });});app.listen(port, () => {  console.log(`server is starting at http://localhost:${port}`);});" > src/index.ts && echo import dotenv from "dotenv";dotenv.config();const {  PORT,  PG_HOST,  PG_DB,  PG_USER,  PG_PASS,  ENV,  PG_DB_TEST,  BCRYPT_PASS,  SALT_ROUND,  TOKEN_SECRET,} = process.env;export default {  PORT: PORT,  HOST: PG_HOST,  DB: ENV === "dev" ? PG_DB : PG_DB_TEST,  USER: PG_USER,  PASS: PG_PASS,  ENV: ENV,  bcrypt: BCRYPT_PASS,  salt: SALT_ROUND,  token: TOKEN_SECRET,}; > src/config.ts